from typer.testing import CliRunner

from {{ package_name }}.cli import app

runner = CliRunner()


def test_example_command_echoes_query() -> None:
    result = runner.invoke(app, ["widget"])
    assert result.exit_code == 0
    assert "widget" in result.stdout
